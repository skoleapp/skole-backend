{
  "name": "skole-monorepo",
  "version": "1.0.0",
  "license": "ISC",
  "author": "Skole Team",
  "scripts": {
    "preinstall": "pip install --upgrade pip",
    "install": "pip install -r requirements.txt -r requirements-dev.txt",
    "make-migrations": "python manage.py makemigrations",
    "clear-migrations": "find . -path '*/migrations/*.py' -not -name '__init__.py' -delete && find . -path '*/migrations/*.pyc'  -delete",
    "migrate": "python manage.py migrate",
    "create-superuser": "python manage.py createsuperuser",
    "import-test-data": "python manage.py loaddata test-data.yaml",
    "test": "mypy . && pytest --cov-report html --cov=. tests"
  }
}
